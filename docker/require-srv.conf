# ------------------------------------------------------------------------
# THIS FILE HAS BEEN AUTOMATICALLY GENERATED VIA SALT-BASED CONFIGURATION.
# ANY MANUAL CHANGES WILL BE OVERWRITTEN!
# ------------------------------------------------------------------------

# The image store and all the container data live under /srv, which is a separate filesystem. Without this dependency a boot on which /srv fails to mount starts
# the daemon against the empty mountpoint on the root filesystem, where the containers initialize fresh, empty data directories instead of failing visibly.
[Unit]
RequiresMountsFor=/srv
